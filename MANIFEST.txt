SecurityIntelligenceBrief 6.1.4 — manual apply package
=======================================================

Baseline this was built against: main @ current 6.1.3 push (VERSION 6.1.3).
Verified before packaging: 122/122 tests pass, 0 Bandit findings, 0 known
dependency CVEs (pip-audit clean).

HOW TO APPLY
------------
1. In your local clone, create/checkout a feature branch (e.g.
   `feature/v6.1.4`) per the repo's engineering rule to use feature branches
   rather than direct changes on main.
2. Copy every file below into the matching path in your clone, overwriting
   the existing file.
3. DELETE these files/directories — they no longer exist in this package
   because they were removed:
     - src/Archive/                          (entire directory — dead code)
     - tests/tests/test_priority_vendor_sources.py   (leftover nested
       duplicate; the corrected version now lives at
       tests/test_priority_vendor_sources.py, included below)
4. Run: PYTHONPATH=src python -m unittest discover -s tests -v
   Expect: 122 tests, OK.
5. Run: bandit -r src/ -ll -q
   Expect: 0 findings.
6. Commit as: "6.1.4 - Nordics, IT/OT source integrity, AI Security &
   Trustworthiness, sector relevance" (see CHANGELOG.md for full detail).
7. IMPORTANT — operational requirement before this is fully live:
   Add `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` to the env block of both
   .github/workflows/daily-security-brief.yml and
   .github/workflows/weekly-vulnerability-report.yml. This is GitHub's
   auto-provided per-run token (no new secret to create) — without it,
   cisa_csaf.py and ai_security_trackers.py will hit the 60/hour
   unauthenticated GitHub API rate limit during catch-up windows.

FILES IN THIS PACKAGE (21)
---------------------------
MODIFIED:
  CHANGELOG.md
  MAINTENANCE.md
  README.md
  VERSION
  src/security_brief/analysis.py
  src/security_brief/app.py
  src/security_brief/priority_vendor_sources.py
  src/security_brief/rendering.py
  src/security_brief/report_policy.py
  src/security_brief/rules.py
  src/security_brief/source_resilience.py
  src/security_brief/sources.py
  src/security_brief/threat_activity.py
  src/security_brief/vulnerability_reporting.py
  tests/test_priority_vendor_sources.py   (replaces the version in tests/tests/)
  tests/test_smoke.py
  tests/test_v6_1_3_intelligence_quality.py

NEW:
  src/security_brief/ai_security_trackers.py
  src/security_brief/cisa_csaf.py
  tests/test_ai_security_trackers.py
  tests/test_cisa_csaf.py

DELETE (not in package, must be removed manually):
  src/Archive/  (whole directory)
  tests/tests/test_priority_vendor_sources.py  (leftover, promoted copy above)

See CHANGELOG.md's "6.1.4" section for the full itemised list of what changed
and why. See MAINTENANCE.md's Priority 2/3 sections for what's still open
(GITHUB_TOKEN requirement, Weekly-report AI tagging, Symantec/Broadcom block,
unverified HTML selectors on Anthropic News and Salesforce Security Blog).
