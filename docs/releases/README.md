# Release records

This directory contains the immutable human-readable release notes for the
Daily Security Brief. Machine-readable manifests are in `manifests/`.

`VERSION` remains the current runtime version. `CHANGELOG.md` contains the
released-history summary, while `MAINTENANCE.md` contains open work only.
